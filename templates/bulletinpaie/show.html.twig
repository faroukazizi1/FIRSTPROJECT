{% extends 'base.html.twig' %}

{% block title %}Bulletinpaie{% endblock %}

{% block body %}
    <h1>Bulletinpaie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bulletinpaie.id }}</td>
            </tr>
            <tr>
                <th>Employe_id</th>
                <td>{{ bulletinpaie.employeId }}</td>
            </tr>
            <tr>
                <th>Mois</th>
                <td>{{ bulletinpaie.mois }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ bulletinpaie.annee }}</td>
            </tr>
            <tr>
                <th>Salaire_brut</th>
                <td>{{ bulletinpaie.salaireBrut }}</td>
            </tr>
            <tr>
                <th>Deductions</th>
                <td>{{ bulletinpaie.deductions }}</td>
            </tr>
            <tr>
                <th>Salaire_net</th>
                <td>{{ bulletinpaie.salaireNet }}</td>
            </tr>
            <tr>
                <th>Date_generation</th>
                <td>{{ bulletinpaie.dateGeneration ? bulletinpaie.dateGeneration|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bulletinpaie_index') }}">back to list</a>

    <a href="{{ path('app_bulletinpaie_edit', {'id': bulletinpaie.id}) }}">edit</a>

    {{ include('bulletinpaie/_delete_form.html.twig') }}
{% endblock %}
