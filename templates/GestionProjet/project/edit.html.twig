

{% block body %}
  {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="modal-footer border-0">
      <button class="btn btn-primary">{{ button_label|default('Update') }}</button>
    <div>
{{ form_end(form) }} 
{% endblock %}
