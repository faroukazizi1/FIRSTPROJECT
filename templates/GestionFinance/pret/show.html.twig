{% extends 'FrontOffice/front_base.html.twig' %}

{% block title %}Pret{% endblock %}

{% block body %}
    <h1>Pret</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID_pret</th>
                <td>{{ pret.IDPret }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ pret.cin }}</td>
            </tr>
            <tr>
                <th>Montant_pret</th>
                <td>{{ pret.MontantPret }}</td>
            </tr>
            <tr>
                <th>Date_pret</th>
                <td>{{ pret.DatePret ? pret.DatePret|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>TMM</th>
                <td>{{ pret.TMM }}</td>
            </tr>
            <tr>
                <th>Taux</th>
                <td>{{ pret.Taux }}</td>
            </tr>
            <tr>
                <th>Revenus_bruts</th>
                <td>{{ pret.RevenusBruts }}</td>
            </tr>
            <tr>
                <th>Age_employe</th>
                <td>{{ pret.AgeEmploye }}</td>
            </tr>
            <tr>
                <th>Duree_remboursement</th>
                <td>{{ pret.dureeRemboursement }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ pret.Categorie }}</td>
            </tr>
        </tbody>
    </table>

    <div style="margin-top: 20px;">
        <a href="{{ path('pret_calendar') }}" target="_blank" class="btn btn-info" style="margin-right: 10px;">
            📅 Voir le calendrier des prêts
        </a>


        <a href="{{ path('app_pret_index') }}" class="btn btn-secondary" style="margin-right: 10px;">
            Retour à la liste
        </a>

        <a href="{{ path('app_pret_edit', {'id': pret.IDPret}) }}" class="btn btn-warning" style="margin-right: 10px;">
            Modifier
        </a>

        {{ include('GestionFinance/pret/_delete_form.html.twig') }}

    
    </div>
{% endblock %}