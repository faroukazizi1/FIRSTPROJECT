{% extends 'FrontOffice/front_base.html.twig' %}

{% block title %}New Pret{% endblock %}

{% block body %}

    <style>
        body {
            background-color: #343a40; /* Fond sombre */
            color: #ffffff; /* Texte blanc */
        }

        h1 {
            font-size: 16px;
            color: #ffffff; /* Titre en blanc */
            margin-bottom: 10px;
        }

        .btn-container {
            display: flex;
            gap: 10px;
            margin-top: 10px;
        }

        .btn {
            font-size: 10px;
            padding: 3px 6px;
            text-decoration: none;
            border-radius: 3px;
            border: 1px solid #ccc;
        }

        a.btn {
            background-color: #ddd;
            color: black;
            border: 1px solid #ccc;
            border-radius: 3px;
        }

        a.btn:hover {
            background-color: #5bc0de; /* Changement de couleur au survol */
            color: #ffffff;
        }

        .btn:active {
            background-color: #46b8da; /* Changement de couleur lorsque le bouton est actif */
        }
    </style>

    <h1>Create new Pret</h1>

    <a href="{{ path('pret_calendar') }}" target="_blank" style="display: inline-block; margin-bottom: 15px; color: blue;">
        📅 Voir le calendrier des prêts
    </a>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}

        <button type="submit" name="simuler" class="btn btn-info">Simuler mon prêt</button>
        <button type="submit" name="submit" class="btn btn-success">Valider</button>
    {{ form_end(form) }}

    {% if simulation %}
        <h3>Résultat de la simulation :</h3>
        <ul>
            <li><strong>Montant maximum autorisé :</strong> {{ simulation.montantMax }} TND</li>
            <li><strong>Taux appliqué :</strong> {{ simulation.taux }}%</li>
            <li><strong>Mensualité estimée :</strong> {{ simulation.mensualite }} TND</li>
            <li><strong>Éligible :</strong> {{ simulation.eligible }}</li>
        </ul>
    {% endif %}

    <div class="btn-container">
        <a href="{{ path('app_pret_index') }}" class="btn" style="color: white; background-color: #6c757d;">Back to list</a>
    </div>

{% endblock %}
