<form method="post" action="{{ path('app_reponse_delete', {'ID_reponse': reponse.ID_reponse}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse.ID_reponse) }}">
    <button class="btn" type="submit">🗑️</button>
</form>
