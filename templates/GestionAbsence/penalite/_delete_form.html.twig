<form method="post" action="{{ path('app_penalite_delete', {'ID_pen': penalite.iD_pen}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette pénalité ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ penalite.iD_pen) }}">
    <button class="btn btn-sm btn-danger">Supprimer</button>
</form>
