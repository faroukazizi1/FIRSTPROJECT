{% extends 'base.html.twig' %}

{% block title %}Demandeconge{% endblock %}

{% block body %}
    <h1>Demandeconge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demandeconge.id }}</td>
            </tr>
            <tr>
                <th>EmployeId</th>
                <td>{{ demandeconge.employeId }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ demandeconge.dateDebut ? demandeconge.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ demandeconge.dateFin ? demandeconge.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>TypeConge</th>
                <td>{{ demandeconge.typeConge }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ demandeconge.statut }}</td>
            </tr>
            <tr>
                <th>DateDemande</th>
                <td>{{ demandeconge.dateDemande ? demandeconge.dateDemande|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_demande_conge_index') }}">back to list</a>

    <a href="{{ path('app_demandeconge_edit', {'id': demandeconge.id}) }}">edit</a>

    {{ include('demandeconge/_delete_form.html.twig') }}
{% endblock %}