<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Liste des Formations | Hirevac</title>

    <!-- Fonts + Bootstrap + Icons -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f4f6f8;
        }

        .navbar {
            background-color: #ffffff;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        }

        .navbar-brand span {
            font-weight: bold;
            font-size: 24px;
            color: #0d6efd;
        }

        .nav-link {
            margin-right: 15px;
            color: #333 !important;
            font-weight: 500;
        }

        .nav-link:hover {
            color: #0d6efd !important;
        }

        h1 {
            margin-top: 60px;
            font-weight: 700;
            text-align: center;
        }

        .formations {
            display: flex;
            flex-wrap: wrap;
            gap: 30px;
            justify-content: center;
            padding: 40px 20px;
        }

        .card {
            border: none;
            border-radius: 16px;
            width: 320px;
            background: #fff;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
            transition: transform 0.2s ease;
            position: relative;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .card img {
            border-top-left-radius: 16px;
            border-top-right-radius: 16px;
            max-height: 180px;
            object-fit: cover;
        }

        .card-body {
            padding: 20px;
        }

        .card-title {
            font-size: 20px;
            font-weight: 700;
            color: #0d6efd;
        }

        .card-text {
            font-size: 14px;
            color: #555;
        }

        footer {
            margin-top: 60px;
            padding: 20px;
            background: #fff;
            text-align: center;
            color: #888;
            border-top: 1px solid #ddd;
        }
    </style>
</head>
<body>

<!-- Navbar moderne -->
<header class="header_section">
    <nav class="navbar navbar-expand-lg navbar-light sticky-top">
        <div class="container">
            <a class="navbar-brand" href="#"><span>Hirevac</span></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav align-items-center">
                    <li class="nav-item"><a class="nav-link" href="#">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">À propos</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Jobs</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Freelancers</a></li>
                    <li class="nav-item"><a class="nav-link" href="#"><i class="fas fa-sign-in-alt me-1"></i>Login</a></li>
                    <li class="nav-item"><a class="nav-link" href="#"><i class="fas fa-user-plus me-1"></i>Sign Up</a></li>
                    <form class="d-flex ms-3">
                        <input class="form-control me-2" type="search" placeholder="Rechercher..." aria-label="Search">
                        <button class="btn btn-outline-primary" type="submit"><i class="fas fa-search"></i></button>
                    </form>
                </ul>
            </div>
        </div>
    </nav>
</header>

<!-- Titre -->
<h1>Nos Formations Disponibles</h1>

<!-- Liste des formations -->
<div class="formations">
    {% for formation in formations %}
        <div class="card">
            {% if formation.Image %}
                <img src="{{ formation.Image }}" class="card-img-top" alt="{{ formation.Titre }}">
            {% else %}
                <img src="https://via.placeholder.com/320x180?text=Aucune+Image" class="card-img-top" alt="No image">
            {% endif %}
            <div class="card-body">
                <h5>
                    <a href="{{ path('front_formation_detail', { id: formation.id_form }) }}" class="text-decoration-none text-primary">
                        {{ formation.Titre }}
                    </a>
                </h5>
                <p class="card-text"><strong>Dates :</strong> du {{ formation.DateD|date('d/m/Y') }} au {{ formation.DateF|date('d/m/Y') }}</p>
                <p class="card-text"><strong>Durée :</strong> {{ formation.Duree }} jours</p>
                <p class="card-text"><strong>Formateur :</strong> {{ formation.formateur.nomF }} {{ formation.formateur.prenomF }}</p>
            </div>
            <!-- Lien cliquable -->
            <a href="{{ path('front_formation_detail', { id: formation.id_form }) }}" class="stretched-link"></a>
        </div>
    {% else %}
        <p>Aucune formation trouvée.</p>
    {% endfor %}
</div>

<!-- Footer simple -->
<footer>
    &copy; {{ "now"|date("Y") }} Hirevac. Tous droits réservés.
</footer>

<!-- JS Bootstrap -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
