<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Liste des Tests</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f4f4f4;
            margin: 0;
            padding: 20px;
        }
        h1 {
            text-align: center;
            color: #333;
        }
        .test-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }
        .test-card {
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            padding: 20px;
            width: 300px;
            transition: transform 0.2s;
        }
        .test-card:hover {
            transform: translateY(-5px);
        }
        .test-title {
            font-size: 1.5em;
            margin-bottom: 10px;
            color: #007bff;
        }
        .test-description {
            margin-bottom: 10px;
            color: #555;
        }
        .test-duration {
            font-weight: bold;
            color: #222;
        }
    </style>
</head>
<body>

<h1>Nos Tests Disponibles</h1>

<div class="test-container">
    {% for test in tests %}
        <div class="test-card">
            <div class="test-title">{{ test.nom }}</div>
            <div class="test-description">{{ test.description }}</div>
            <div class="test-duration">Durée : {{ test.duree }} minutes</div>
            <a href="{{ path('front_test_detail', {'id': test.id}) }}" class="btn btn-primary">Évaluer</a>
        </div>
    {% else %}
        <p>Aucun test trouvé.</p>
    {% endfor %}
</div>

</body>
</html>
