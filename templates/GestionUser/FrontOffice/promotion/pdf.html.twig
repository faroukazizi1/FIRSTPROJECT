{# templates/promotion/pdf.html.twig #}
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Promotion PDF</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        h1 { color: #333; }
        p { margin: 5px 0; }
    </style>
</head>
<body>
    <h1>Promotion - {{ promotion.postePromo }}</h1>

    <p><strong>Type :</strong> {{ promotion.typePromo }}</p>
    <p><strong>Raison :</strong> {{ promotion.raison }}</p>
    <p><strong>Date :</strong> {{ promotion.datePromo ? promotion.datePromo|date('Y-m-d') : 'N/A' }}</p>
    <p><strong>Nouveau Salaire :</strong> {{ promotion.nouvSal }} €</p>
    <p><strong>Avantages :</strong> {{ promotion.avantageSupp }}</p>
</body>
</html>
